import { FC } from 'react';

import style from './User.module.scss';

const User: FC = () => {
  return (
    <div className={style['user-page']}>
      <div></div>
    </div>
  );
};

export default User;
